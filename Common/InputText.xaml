<Window x:Class="NirIdentifier.Common.InputText"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:btn="clr-namespace:Ai.Hong.CommonLibrary.Controls;assembly=CommonLibrary"
        Title="输入" Height="180" Width="350" WindowStartupLocation="CenterOwner" ResizeMode="NoResize" Loaded="Window_Loaded">
    <Grid Margin="10" TextBlock.FontSize="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Name="txtTitle" Grid.Row="0" Text="请输入内容" HorizontalAlignment="Center"/>
        <TextBox Name="txtInput" Grid.Row="1" Margin="0,10,0,0"/>
        <btn:OkCancelPanel x:Name="BtnOkCancel" Grid.Row="2"  Margin="0,20,0,0" CancelClicked="BtnOkCancel_CancelClicked" OKClicked="BtnOkCancel_OKClicked" />
    </Grid>
</Window>
